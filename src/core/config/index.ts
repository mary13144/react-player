import { LanguageType } from '@/types';
import { ToastPosition } from '@/components/toast';

/**
 * @description 默认初始音量60
 */
export const defaultVolume: number = 60;

export const defaultTheme: string = 'red';

export const defaultLanguage: LanguageType = 'zh';

export const defaultToastPosition: ToastPosition = 'leftTop';
